import React from 'react';
import Hero from '../hero/Hero';
import MovieGrid from '../moviegrid/MovieGrid';

const Home = () => {
    return (
        <div>
            <Hero />
            <MovieGrid />
        </div>
    );
};

export default Home;
